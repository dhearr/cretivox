import { styles } from "@/app/styles";
import React from "react";

const loading = () => {
  return (
    <div className={styles.containLoading}>
      <div className={styles.animateLoading}>Cretivox</div>
    </div>
  );
};

export default loading;
